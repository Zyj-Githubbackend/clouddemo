我已经完成了新的页面设计，并且已经拿到了每个页面对应的 HTML / CSS / 资源文件。  文件放在stitch_markdown_diagram_renderer目录下
现在请你不要重新设计页面，而是基于这些现成的 HTML，在当前项目中完成 Vue 前端重构，并逐步与后端接口对接。

总体目标：
1. 以这些 HTML 为基础，在当前项目创建frontend2，并在其中重构出新的 Vue项目。
2. 严格沿用当前项目已有技术栈、目录结构、路由体系、请求封装方式、状态管理方式、权限控制方式。
3. 在尽量保留现有 HTML 视觉结构与样式效果的前提下，改造成可维护的 Vue 组件。
4. 最终完成前后端联调，而不是只做静态页面。

请严格遵守以下原则：
- 以当前仓库代码为准，不要凭空假设项目结构。
- 不要重写整个前端框架，优先融入现有项目。
- 不要只把 HTML 原样贴进去，要拆分成合理的 Vue 页面和组件。
- 不要虚构后端接口；如果接口不存在，请明确标记缺口。
- 如果 HTML 与现有业务逻辑冲突，以业务逻辑和现有后端接口为准，对 HTML 做必要微调。
- 每一步修改都尽量可运行、可验证。

请按以下阶段执行：

第一阶段：项目接入分析
- 扫描当前前端项目结构
- 识别技术栈（Vue2/Vue3、Options API/Composition API、Vite/Webpack、Pinia/Vuex、Axios/request 封装、UI 组件库）
- 识别路由结构、布局结构、权限守卫、公共组件、API 目录、样式组织方式
- 输出“新页面接入方案”

第二阶段：HTML → Vue 重构
针对我提供的 HTML 页面：
- 将页面按当前项目规范改造成 Vue 页面
- 抽取可复用组件，例如：
  - 页面容器
  - 查询表单
  - 工具栏
  - 表格区
  - 卡片区
  - 详情弹窗 / 编辑弹窗
  - 分页区
  - 状态标签
- 保留现有 HTML 的 class 结构和样式资源，避免破坏视觉效果
- 清理明显不适合 Vue 项目的纯静态写法
- 把假数据、静态文本、占位区域标记清楚

第三阶段：路由与菜单接入
- 将新页面接入现有 router
- 如果项目有菜单配置、权限路由、动态路由，请按现有机制接入
- 标记新增/修改了哪些路由文件、菜单配置文件、布局文件

第四阶段：前后端接口对接
请根据现有后端代码或前端现有 API 封装，完成以下工作：
- 为每个页面梳理接口映射关系：
  - 页面功能
  - 对应接口
  - 请求方式
  - 请求参数
  - 返回字段
  - 前端字段映射
- 完成典型业务流程对接：
  - 列表查询
  - 条件筛选
  - 分页
  - 详情获取
  - 新增
  - 编辑回显
  - 删除
  - 状态修改
  - 批量操作（如果页面需要）
- 加入页面状态处理：
  - loading
  - empty
  - error
  - submit pending
  - 无权限
- 对表单做基础校验
- 对接口异常做统一提示处理

第五阶段：输出结果
请输出：
1. 页面接入与重构说明
2. 新增/修改文件清单
3. 组件拆分说明
4. 路由接入说明
5. API 对接说明
6. 字段映射说明
7. 当前仍缺失的接口/字段/业务点
8. 可直接提交的代码修改

工作方式要求：
- 优先从一个页面开始打通完整链路，再批量迁移其它页面
- 每完成一个页面，说明：
  - 已完成哪些静态重构
  - 已对接哪些接口
  - 还缺哪些接口或字段
  - 是否可以运行验证
- 如果仓库中已有相似旧页面，优先复用其接口、校验、权限、状态处理逻辑